               <%= label_tag "#{field.abstract_model.to_param}_#{field.name}", field.label %>
               <%= text_field field.abstract_model.to_param, field.name, :maxlength => field.length, :class => "floatField #{field.has_errors? ? "errorField" : nil}", :value => field.value %>
