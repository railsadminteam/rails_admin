                <%= label_tag "#{field.name}", field.label %>
                <%= text_field field.abstract_model.to_param, field.name, :maxlength => field.length, :class => "#{field.css_class} #{field.has_errors? ? "errorField" : nil}", :value => field.formatted_value %>
