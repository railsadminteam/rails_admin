                <%= label_tag "#{field.abstract_model.to_param}_#{field.name}", field.label %>
                <%= text_field field.abstract_model.to_param, field.name, :size => [50, field.length].min, :maxlength => field.length, :class => "#{field.css_class} #{field.has_errors? ? "errorField" : nil} #{field.color ? 'color' : nil}", :value => field.value %>
