                <%= label_tag "#{field.name}", field.label %>
                <%= text_field field.abstract_model.to_param, field.name, :size => [50, field.length].min, :maxlength => field.length, :class => "stringField #{field.has_errors ? "errorField" : nil}", :value => field.value %>
