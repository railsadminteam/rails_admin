                <%= label_tag "#{field.to_param}_localized", field.label %>
                <%= text_field field.abstract_model.to_param, "#{field.name}_localized", :class => "#{field.css_class} #{field.has_errors? ? "errorField" : nil}", :value => field.formatted_value, :style => "width:#{field.column_width}px" %>
                <%= hidden_field field.abstract_model.to_param, field.name %>
                <script type="text/javascript">
                $j(document).ready(function($){
                  $("#<%= "#{field.to_param}_localized" %>").datepicker({
                    altField: '#<%= "#{field.abstract_model.to_param}_#{field.name}" %>',
                    altFormat: "yy-mm-dd",
                    dateFormat: "dd.mm.yy"
                  });
                });
                </script>