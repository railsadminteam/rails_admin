= form.fields_for field.name do |nested_form|
  %blockquote
    = nested_form.link_to_remove "Remove", :class => 'label notice'
    = nested_form.generate({:action => :create, :model_config => field.associated_model_config, :nested_in => field.name })
    %hr
= form.link_to_add "Add a #{field.associated_model_config.abstract_model.pretty_name.downcase}", field.name, :class => 'label notice'
