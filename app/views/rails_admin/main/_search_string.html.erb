      <div class="search field <%= field.to_param %>">
          <%= label_tag field.to_param, field.label, {:class => "main_label" }   %>
          <input type="text" name="query[<%=field.name%>]" value="<%= query[field.name] unless query.nil? %>" size="32" />
      </div>
