#header
  %h1
  -# TODO: make the logo customisable and link to the dashboard
  
  #user-navigation
    %ul.wat-cf
      %li= link_to icon(:config, "Dashboard"), rails_admin_dashboard_path
      - if (root_path rescue nil)
        %li= link_to icon(:home, t('home.name').capitalize), root_path
      - if (current_user rescue nil)
        %li= link_to icon(:account, current_user.email), (rails_admin_edit_path(:user, current_user) rescue '#')
      - if (current_user rescue nil)
        %li= link_to icon(:logout, t("admin.credentials.log_out")), (destroy_session_path(current_user) rescue '#')
      