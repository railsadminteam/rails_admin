<%# Override this partial in your application to add your own <link>/<script>
    tags (or inline styles/scripts) to the RailsAdmin <head>. It is rendered
    right after the bundled RailsAdmin assets, regardless of asset_source. %>
